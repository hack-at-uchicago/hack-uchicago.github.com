head.ready(function() {
  // All of our JavaScript libraries are loaded! Let's get to work...

  // TimeAgo coverts a specially formatted tag into an automagically-updating timestamp
  $('.time-ago').timeago();
});